rest_command:
  jarvis_assist:
    url: "http://YOUR-LLM-HOST:8787/api/assist"
    method: post
    content_type: "application/json"
    headers:
      Authorization: "Bearer !secret jarvis_assist_token"
    payload: >
      {
        "source": "assist",
        "text": "{{ text }}"
      }
